// CONSTANTE
// FIM CONSTANTE

// ATRIBUTOS GLOBAIS
// FIM ATRIBUTOS GLOBAIS

// CLASSE
function WsObjetoMain() {
	// HERANÇA

	Objeto.apply(this);
	
	// FIM HERANÇA

	// ATRIBUTO

	this._intMensagemId;
	this.getIntMensagemId = function() {
		return this._intMensagemId;
	}
	this.setIntMensagemId = function(intMensagemId) {
		this._intMensagemId = intMensagemId;
	}

	this._strJsonFilho;
	this.getStrJsonFilho = function() {
		return this._strJsonFilho;
	}
	this.setStrJsonFilho = function(strJsonFilho) {
		this._strJsonFilho = strJsonFilho;
	}

	// FIM ATRIBUTO
	
	// MÉTODO
	// FIM MÉTODO

	/* Construtor */
	{
		// VARIÁVEL
		// AÇÃO
	}

}

// INICIALIZAÇÃO
// FIM INICIALIZAÇÃO
